IDENTIFICATION DIVISION.
    PROGRAMID. FIB.
    AUTHOR. ANONYMOUS.
    DATA-WRITTEN. 2019-11-01.

PROCEDURE DIVISION.
MAIN.
    MOVE 15 TO x
    MOVE 1 TO Y

    IF x = 10 OR 15 THEN
        DISPLAY "WORKING"
    ELSE
        DISPLAY "NOT WORKING"
    END.

    IF x > 10 AND < 15 THEN
         DISPLAY "WORKING"
    ELSE
         DISPLAY "NOT WORKING"
    END.

    IF x > 10 AND < 20 OR 100 AND > 80 THEN
         DISPLAY "WORKING"
    ELSE
         DISPLAY "NOT WORKING"
    END
.